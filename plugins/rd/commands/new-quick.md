---
description: "创建 QUICK 需求文档和可执行方案，后续由 dev、test、done 完成生命周期。"
argument-hint: "[标题] [--from-issue=#编号]"
---

Before executing, read [command model routing](../shared/_command-models.md) for `rd:new-quick` (execution tier: `standard`). An already delegated executor must not route again.

Use the `rd:new-quick` skill and follow its `SKILL.md` instructions.
Related helper skills: `rd:quick-fix-guide`.


User arguments:

```text
$ARGUMENTS
```

If no arguments are supplied, follow the skill's no-argument behavior.
