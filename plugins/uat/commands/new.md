---
description: "创建测试流程文档 - 多轮对话引导生成"
argument-hint: "[arguments]"
---

Before executing, read [command model routing](../shared/_command-models.md) for `uat:new` (execution tier: `primary`). An already delegated executor must not route again.

Use the `uat:new` skill and follow its `SKILL.md` instructions.

User arguments:

```text
$ARGUMENTS
```

If no arguments are supplied, follow the skill's no-argument behavior.
