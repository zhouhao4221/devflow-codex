---
description: "初始化 UAT 插件 - 创建目录结构并安装 skill 到项目"
argument-hint: "[arguments]"
---

Use the `uat:init` skill and follow its `SKILL.md` instructions.
Related helper skills: `uat:uat-executor`.


User arguments:

```text
$ARGUMENTS
```

If no arguments are supplied, follow the skill's no-argument behavior.
