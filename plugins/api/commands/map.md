---
description: "字段映射 - 分析接口请求/响应字段并映射到前端类型"
argument-hint: "[arguments]"
---

Use the `api:map` skill and follow its `SKILL.md` instructions.
Related helper skills: `api:api-field-mapper`.


User arguments:

```text
$ARGUMENTS
```

If no arguments are supplied, follow the skill's no-argument behavior.
