# Diag 维护约定

生产诊断默认只读，定位后输出修复建议，不自动修改远端服务。保留主机、服务和时间范围约束，不绕过环境权限审批。

Codex 版不默认安装 Claude 的安全 hooks，也不能在文档中声称这些 hooks 正在保护当前执行。只有实际分发并显式启用的检查才可描述为自动执行。

审阅 SSH 流程时同时检查脚本是否随包存在、工具能力是否可用，以及报告是否对应真实执行证据。默认把日志流回本地处理；远端临时文件写入和清理也是远端修改，不能归入“全程只读”。将这些边界写入相关技能，不能仅依赖本维护文件。
