---
description: "生产诊断插件 - 列出 init / diagnose / audit 子命令"
argument-hint: "[arguments]"
---

Use the `diag:diag` skill and follow its `SKILL.md` instructions.

User arguments:

```text
$ARGUMENTS
```

If no arguments are supplied, follow the skill's no-argument behavior.
