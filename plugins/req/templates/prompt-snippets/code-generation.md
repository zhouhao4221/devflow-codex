# 代码生成

> 本文件由 `/req:dev` 读取，注入项目特有的代码生成规范。
> 删除不需要的章节即可——缺失时命令按 `architecture.md` 通用规则生成。

## 什么时候用

> 说明哪些开发场景应参照本规范，哪些不适用。

<!--
示例：
- 新增 CRUD 资源模块、为已有模块加接口时参照
- 纯前端样式微调、脚本类一次性任务不必参照
-->

## 必备输入

> 触发前必须准备的清单。缺输入会导致生成质量明显下降。

<!--
示例：
- 模块名 / 资源名 / 业务动作名
- 请求、响应字段定义（或 Swagger 引用）
- 权限标识格式，如 `module:resource:action`
- 是否涉及事务 / 消息队列 / 外部接口
-->

## 触发方式

> 如何触发本规范，或写入 AGENTS.md 持续生效。

<!--
示例：
- `/req:dev REQ-XXX`
- 在 AGENTS.md「代码生成」章节引用 `docs/prompt/code-generation.md`
-->

## 优质输出标准

> 好的生成结果应满足的条件。

<!--
示例：
- 遵循项目分层（如 Model → Store → Biz → Controller → Router）
- Controller 只做请求解析与响应，业务规则下沉到 Biz
- 路由挂载认证 / 操作日志 / 权限中间件
- 新功能附带必要的测试
-->

## 常见失败模式

| 问题 | 原因 | 解决方案 |
|------|------|----------|
<!-- 示例：Biz 中直接 new 具体 Store | 无法 mock | 定义最小接口注入 -->
