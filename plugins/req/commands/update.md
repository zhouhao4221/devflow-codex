---
description: "热更新插件 - 拉取最新命令文件和技能，所有项目立即生效"
argument-hint: "[arguments]"
---

Use the `req:update` skill and follow its `SKILL.md` instructions.

User arguments:

```text
$ARGUMENTS
```

If no arguments are supplied, follow the skill's no-argument behavior.
