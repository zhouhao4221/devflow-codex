---
description: "初始化需求项目 - 创建本地需求存储和 DevFlow 配置"
argument-hint: "[arguments]"
---

Use the `req:init` skill and follow its `SKILL.md` instructions.

User arguments:

```text
$ARGUMENTS
```

If no arguments are supplied, follow the skill's no-argument behavior.
