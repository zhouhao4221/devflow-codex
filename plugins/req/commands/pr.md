---
description: "创建 PR - 根据仓库类型自动创建 Pull Request"
argument-hint: "[arguments]"
---

Use the `req:pr` skill and follow its `SKILL.md` instructions.

User arguments:

```text
$ARGUMENTS
```

If no arguments are supplied, follow the skill's no-argument behavior.
