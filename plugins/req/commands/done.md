---
description: "完成需求 - 标记完成并归档"
argument-hint: "[arguments]"
---

Use the `req:done` skill and follow its `SKILL.md` instructions.

User arguments:

```text
$ARGUMENTS
```

If no arguments are supplied, follow the skill's no-argument behavior.
