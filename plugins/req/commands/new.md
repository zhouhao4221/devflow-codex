---
description: "创建新需求 - 基于模板创建需求文档"
argument-hint: "[arguments]"
---

Use the `req:new` skill and follow its `SKILL.md` instructions.
Related helper skills: `req:requirement-analyzer`.


User arguments:

```text
$ARGUMENTS
```

If no arguments are supplied, follow the skill's no-argument behavior.
