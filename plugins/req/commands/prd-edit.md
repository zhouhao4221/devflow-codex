---
description: "编辑 PRD - 修改和完善产品需求文档"
argument-hint: "[arguments]"
---

Use the `req:prd-edit` skill and follow its `SKILL.md` instructions.
Related helper skills: `req:prd-analyzer`.


User arguments:

```text
$ARGUMENTS
```

If no arguments are supplied, follow the skill's no-argument behavior.
