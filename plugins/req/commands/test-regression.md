---
description: "回归测试 - 运行已有自动化测试用例"
argument-hint: "[arguments]"
---

Use the `req:test-regression` skill and follow its `SKILL.md` instructions.
Related helper skills: `req:test-guide`.


User arguments:

```text
$ARGUMENTS
```

If no arguments are supplied, follow the skill's no-argument behavior.
