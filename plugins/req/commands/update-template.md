---
description: "更新模板 - 将插件最新模板同步到项目本地"
argument-hint: "[arguments]"
---

Use the `req:update-template` skill and follow its `SKILL.md` instructions.

User arguments:

```text
$ARGUMENTS
```

If no arguments are supplied, follow the skill's no-argument behavior.
