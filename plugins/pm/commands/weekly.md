---
description: "周报 - 基于项目数据自动生成本周工作汇报"
argument-hint: "[arguments]"
---

Before executing, read [command model routing](../shared/_command-models.md) for `pm:weekly` (execution tier: `standard`). An already delegated executor must not route again.

Use the `pm:weekly` skill and follow its `SKILL.md` instructions.

User arguments:

```text
$ARGUMENTS
```

If no arguments are supplied, follow the skill's no-argument behavior.
