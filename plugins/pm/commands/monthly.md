---
description: "月报 - 月度工作总结与统计分析"
argument-hint: "[arguments]"
---

Before executing, read [command model routing](../shared/_command-models.md) for `pm:monthly` (execution tier: `standard`). An already delegated executor must not route again.

Use the `pm:monthly` skill and follow its `SKILL.md` instructions.

User arguments:

```text
$ARGUMENTS
```

If no arguments are supplied, follow the skill's no-argument behavior.
